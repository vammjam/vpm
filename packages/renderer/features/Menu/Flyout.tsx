import styled from 'styled-components'

export default function Flyout(): JSX.Element {
  return <Container>Flyout</Container>
}

const Container = styled.div`
  position: fixed;
  top: -100%;
  width: 100%;
  padding: 1rem;
`
